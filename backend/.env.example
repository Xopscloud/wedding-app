# Backend env example
PORT=4000
ADMIN_PASSWORD=changeme
# Optional: restrict CORS origin (e.g. http://localhost:3000)
CORS_ORIGIN=http://localhost:3000
# Optional: host name for logging only
BACKEND_HOST=localhost

# Optional: AWS S3 configuration for direct uploads
# If these are set, the backend will enable S3 uploads and provide presigned URLs for direct upload.
S3_BUCKET=your-s3-bucket-name
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIA...REPLACE
AWS_SECRET_ACCESS_KEY=your-secret
